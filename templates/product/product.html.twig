{% extends 'base.html.twig' %}

{% block title %}{{ product.label }}{% endblock %}

{% block body %}
    <main id="product-page-content">
        <figure class="half-screen-img">
            <img src="{{ asset('img/home_1.jpg') }}" alt="Image {{ product.label }}">
        </figure>
        <div class="product-container">
            <div class"product-info">
                <h1>{{ product.label }}</h1>
                <span>{{ product.price|number_format(2, ',', '.') }}</span>
                <p>{{ product.description }}</p>
            </div>
            {% if user !== null %}
                <a class="btn" href="{{ path("app_command_add", {id: product.id}) }}">Ajouter aux panier</a>
            {% else %}
                <a class="btn" href="{{ path("app_login") }}">Connexion</a>
            {% endif %}
        </div>
    </main>
{% endblock %}
